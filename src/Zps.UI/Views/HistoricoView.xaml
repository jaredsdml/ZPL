<UserControl x:Class="Zps.UI.Views.HistoricoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Zps.UI.ViewModels"
             d:DataContext="{d:DesignInstance vm:HistoricoViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding BusquedaTexto, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         ToolTip="Buscar por LPN, Arribo, Cliente, Solicitante, Tipo, SKU o Lote..." />

                <Button Grid.Column="2" Content="🔄 Sincronizar (F5)" Command="{Binding SincronizarCommand}" />
                <Button Grid.Column="4" Style="{StaticResource SecondaryButton}" Content="🖨️ Reimprimir Selección" Command="{Binding ReimprimirSeleccionCommand}" />
                <Button Grid.Column="6" Style="{StaticResource SecondaryButton}" Content="📥 Exportar a Excel" Command="{Binding ExportarExcelCommand}" />
            </Grid>
        </Border>

        <DataGrid Grid.Row="1"
                  x:Name="DataGridHistorico"
                  ItemsSource="{Binding VistaFiltrada}"
                  SelectionMode="Extended"
                  SelectionChanged="DataGridHistorico_OnSelectionChanged"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="LPN" Binding="{Binding Lpn}" Width="180" />
                <DataGridTextColumn Header="Consecutivo" Binding="{Binding Consecutivo}" Width="100" />
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="110" />
                <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="120" />
                <DataGridTextColumn Header="Solicitante" Binding="{Binding Solicitante}" Width="140" />
                <DataGridTextColumn Header="Arribo" Binding="{Binding Arribo}" Width="140" />
                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="130" />
                <DataGridTextColumn Header="Lote" Binding="{Binding Lote}" Width="110" />
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="90" />
                <DataGridTextColumn Header="Fecha/Hora" Binding="{Binding FechaHora}" Width="160" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Margin="0,10,0,0" Text="{Binding EstadoMensaje}" TextWrapping="Wrap" />
    </Grid>
</UserControl>
