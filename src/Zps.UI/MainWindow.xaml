<Window x:Class="Zps.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Zps.UI.ViewModels"
        xmlns:views="clr-namespace:Zps.UI.Views"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Zebra Print Station"
        Height="800" Width="1300"
        MinHeight="600" MinWidth="1000">

    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding Historico.SincronizarCommand}" />
    </Window.InputBindings>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="0,0,0,1"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Zebra Print Station" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" />
                    <TextBlock x:Name="TxtVersion" Style="{StaticResource SubtleTextBlock}" Margin="10,3,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <TextBlock Grid.Column="1" Text="{Binding EstadoConexion}" VerticalAlignment="Center" Margin="0,0,16,0" />

                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}"
                        Content="🌓 Tema"
                        Command="{Binding AlternarTemaCommand}" />
            </Grid>
        </Border>

        <TabControl Margin="8" SelectionChanged="TabControl_OnSelectionChanged">
            <TabItem Header="Generador Principal">
                <views:GeneradorPrincipalView DataContext="{Binding Generador}" />
            </TabItem>
            <TabItem x:Name="TabHistorico" Header="Histórico y Reimpresiones">
                <views:HistoricoView DataContext="{Binding Historico}" />
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
